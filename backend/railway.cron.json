{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "npm run db:seed",
    "restartPolicyType": "NEVER",
    "cronSchedule": "52 21 * * *"
  }
}
